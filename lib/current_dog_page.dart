import 'package:flutter/material.dart';

class CurrentDogPage extends StatelessWidget {
  const CurrentDogPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Current dog'),
    );
  }
}
