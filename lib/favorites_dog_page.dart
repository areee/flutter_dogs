import 'package:flutter/material.dart';

class FavoritesDogPage extends StatelessWidget {
  const FavoritesDogPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favorite dogs'),
    );
  }
}
